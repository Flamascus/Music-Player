<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Music_Player.Views.CustomCells.ControlDescriptionView"
             HeightRequest="45">
  <ContentView.Content>
    <Grid x:Name="Grid" Padding="15, 0, 0, 0">

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="auto"/>
      </Grid.ColumnDefinitions>

      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="auto"/>
      </Grid.RowDefinitions>

      <Label x:Name="lblText" VerticalTextAlignment="Center"/>
      <Label x:Name="lblDescription" TextColor="{StaticResource Text2}" IsVisible="False" Grid.Row="1"/>
    </Grid>
  </ContentView.Content>
</ContentView>