<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Music_Player.Services"
             x:Class="Music_Player.Views.TrackPage"
             BackgroundColor="{StaticResource Back}"
             Title="Library">
    <ContentPage.Content>
        <local:GradientColorStack x:Name="Gradient">
            <StackLayout VerticalOptions="CenterAndExpand" Margin="0, 10, 0, 0" Spacing="20">
                <Image Source="options.png" HorizontalOptions="End" WidthRequest="20"/>
                <Grid WidthRequest="300" HeightRequest="300"
                HorizontalOptions="Center"
                VerticalOptions="Center">
                    <Image Source="{Binding CoverSource}" BackgroundColor="{StaticResource Front}" Aspect="AspectFill"/>
                </Grid>
                <Label Text="{Binding Title}" HorizontalOptions="Center"/>
                <Label Text="{Binding Producer}" HorizontalOptions="Center"/>
                <Image Source="{Binding PlayPauseImageSource}" WidthRequest="50">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding PlayTapCommand}" />
                    </Image.GestureRecognizers>
                </Image>
            </StackLayout>
        </local:GradientColorStack>
    </ContentPage.Content>
</ContentPage>